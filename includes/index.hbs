<!DOCTYPE html>
<html lang="en">
  <head>
    <title>●●●●タイトル●●●●</title>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="robots" content="noindex, nofollow" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="shortcut icon" type="image/x-icon" href="favicon.ico" />
    <link href="index.css" rel="stylesheet" />
  </head>

  <body>
    <!-- =============================================
        ニュースフィーダー部分
    ============================================= -->
    <div class="news__unit">
      <div class="news__unit_inner">
        {{#each dates}}
        <div class="news__unit_item">
          <div class="news__unit_item_date">{{isoPublishDate}}</div>
          {{#each articles}}
          <div class="news__unit_item_body">
            <div class="news__unit_item_body_ttl"><a href="{{link}}">{{title}}</a></div>
            <div class="news__unit_item_body_src">Source: <a href="{{source.siteUrl}}">{{source.title}}</a></div>
          </div>
          {{/each}}
        </div>
        {{/each}}
      </div>
    </div>
    <!-- =============================================
        ニュースフィーダー部分
    ============================================= -->
    <script src="index.js"></script>
  </body>
</html>
